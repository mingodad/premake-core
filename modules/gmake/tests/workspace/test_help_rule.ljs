//
// tests/actions/make/test_help_rule.ljs
// Validate generation of help rule and configurations list.
// Copyright (c) 2012-2015 Jason Perkins and the Premake project
//

	var p = premake;
	var suite = test.declare("make_help_rule");


//
// Setup/teardown
//

	var wks;

	function suite.setup() {
		wks = test.createWorkspace();
	}

	var function prepare() {
		wks = test.getWorkspace(wks);
		p.make.helprule(wks);
	}


//
// Start with the default Debug and Release setup.
//

	function suite.looksOkay_onDefaultSetup() {
		prepare();
		test.capture ([=[
help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug"
	@echo "  release"
		]=]);
	}
