//
// tests/actions/make/cpp/test_tools.ljs
// Tests for tools support in makefiles.
// Copyright (c) 2012-2013 Jason Perkins and the Premake project
//

	var p = premake;
	var suite = test.declare("make_tools");
	var make = p.make;
	var cpp = p.make.cpp;
	var project = p.project;


//
// Setup
//

	var cfg;

	function suite.setup() {
		var wks, prj = test.createWorkspace();
		cfg = test.getconfig(prj, "Debug");
	}


//
// Make sure that the correct tools are used.
//

	function suite.usesCorrectTools() {
		make.cppTools(cfg, p.tools.gcc);
		test.capture ([=[
  RESCOMP = windres
		]=]);
	}
