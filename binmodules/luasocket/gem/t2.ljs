source = {};
sink = {};
pump = {};
filter = {};

// filter.chain
dofile("ex3.ljs");

// normalize
require("gem");
eol = gem.eol;
dofile("ex2.ljs");

// encode
require("mime");
encode = mime.encode;

// wrap
wrap = mime.wrap;

// source.chain
dofile("ex6.ljs");

// source.file
dofile("ex5.ljs");

// sink.file
require("ltn12");
sink.file = ltn12.sink.file;

// pump.all
dofile("ex10.ljs");

// run test
CRLF = "\013\010";
dofile("ex4.ljs");
