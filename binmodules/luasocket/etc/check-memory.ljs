function load(s) {
    collectgarbage();
    var a = gcinfo();
    _G[s] = require(s);
    collectgarbage();
    var b = gcinfo();
    print(s .. ":\t " .. (b-a) .. "k");
}

load("socket.url");
load("ltn12");
load("socket");
load("mime");
load("socket.tp");
load("socket.smtp");
load("socket.http");
load("socket.ftp");
